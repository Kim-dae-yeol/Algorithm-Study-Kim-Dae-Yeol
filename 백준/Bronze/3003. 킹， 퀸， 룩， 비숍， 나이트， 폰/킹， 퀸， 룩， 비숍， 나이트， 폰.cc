#include <bits/stdc++.h>
using namespace std;
int main(){
    int k,q,l,kn, b, p;
    cin >> k >> q >> l >> kn >> b >> p;
    cout << 1 - k << " " << 1 - q << " " << 2 - l << " " << 2 - kn << " " << 2 - b << " " << 8 - p;
    return 0;
}